# Archived Overlays

This directory stores deprecated overlay trees for historical traceability only.

- Do not use archived overlays as active task back-link targets.
- Do not reference archived overlays in tasks.json or task view files.
- Active overlays must live under `docs/architecture/overlays/<PRD-ID>/08/`.
